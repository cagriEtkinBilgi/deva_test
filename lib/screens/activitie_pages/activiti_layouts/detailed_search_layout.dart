import 'package:flutter/material.dart';
class DetailedSearchLayout extends StatelessWidget {
  const DetailedSearchLayout({Key key}) : super(key: key);
//detaylı serach form yazılacak!
  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
